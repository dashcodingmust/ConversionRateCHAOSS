#this is the interface
import time_of_last_commit as t
import Number_of_contributions as n

n.investment(input('owner'),input('repo name'))
t.lastCommitTime(input('number of commits'),input('owner'),input('repo name'))
